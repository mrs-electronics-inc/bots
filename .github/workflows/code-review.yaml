name: Code Review

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  run_code_review_bot:
    runs-on: ubuntu-latest
    container: ghcr.io/mrs-electronics-inc/bots/code-review:0.2
    permissions:
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run Code Review Bot
        env:
          OPENROUTER_KEY: ${{ secrets.API_KEY_CODE_REVIEW_BOT }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash /bin/github_code_review.sh
